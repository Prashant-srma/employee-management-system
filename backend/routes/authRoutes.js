const express=require('express');const rateLimit=require('express-rate-limit');const c=require('../controllers/authController');const {authenticateUser}=require('../middleware/auth');const router=express.Router();const limiter=rateLimit({windowMs:15*60*1000,max:20,message:{success:false,message:'Too many login attempts, please try again later'}});router.post('/admin/login',limiter,c.adminLogin);router.post('/employee/login',limiter,c.employeeLogin);router.post('/logout',c.logout);router.get('/me',authenticateUser,c.me);router.post('/change-password',authenticateUser,c.changePassword);module.exports=router;
