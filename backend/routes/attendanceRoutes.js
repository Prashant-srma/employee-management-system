const express=require('express');const c=require('../controllers/attendanceController');const {authenticateUser,requireAdmin,requireEmployee}=require('../middleware/auth');const router=express.Router();router.use(authenticateUser);router.get('/employee/:id',requireAdmin,c.employeeList);router.get('/export',requireAdmin,c.exportCsv);router.get('/',requireAdmin,c.list);router.get('/mine',requireEmployee,c.employeeList);router.post('/clock-in',requireEmployee,c.clockIn);router.post('/clock-out',requireEmployee,c.clockOut);module.exports=router;
