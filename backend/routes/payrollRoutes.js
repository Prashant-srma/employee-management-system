const express=require('express');const c=require('../controllers/payrollController');const {authenticateUser,requireAdmin,requireEmployee}=require('../middleware/auth');const router=express.Router();router.use(authenticateUser);router.get('/mine',requireEmployee,c.mine);router.get('/:id/pdf',c.pdf);router.get('/:id',c.getOne);router.get('/',requireAdmin,c.list);router.post('/',requireAdmin,c.create);module.exports=router;
