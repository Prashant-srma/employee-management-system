const express=require('express');const c=require('../controllers/leaveController');const {authenticateUser,requireAdmin,requireEmployee}=require('../middleware/auth');const router=express.Router();router.use(authenticateUser);router.get('/mine',requireEmployee,c.mine);router.get('/',requireAdmin,c.list);router.post('/',requireEmployee,c.create);router.post('/:id/approve',requireAdmin,c.approve);router.post('/:id/reject',requireAdmin,c.reject);module.exports=router;
