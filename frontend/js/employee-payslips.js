(async()=>{const u=await EMSAuth.requireAuth('employee');if(!u)return;buildShell(u,'employee');page.innerHTML=`<div class="page-header"><div><h1 class="page-title">Payslips</h1><p class="page-subtitle">Your payslip history</p></div></div><div class="table-wrap"><table class="table" id="table"></table></div>`;async function load(){const r=await API.get('/api/payroll/mine');table.innerHTML=`<thead><tr><th>Period</th><th>Basic Salary</th><th>Net Salary</th><th>Status</th><th>Action</th></tr></thead><tbody>${r.data.map(p=>`<tr><td>${Utils.esc(p.period)}</td><td>${Utils.money(p.basicSalary)}</td><td><b>${Utils.money(p.netSalary)}</b></td><td>${Utils.statusBadge(p.status)}</td><td><a class="btn btn-sm" href="/api/payroll/${p._id}/pdf"><i data-lucide="download"></i>Download</a></td></tr>`).join('')||'<tr><td colspan="5">No payslips available.</td></tr>'}</tbody>`;lucide.createIcons()}await load()})();
