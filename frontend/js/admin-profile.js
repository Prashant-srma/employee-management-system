(async()=>{const user=await EMSAuth.requireAuth('admin');if(!user)return;buildShell(user,'admin');const id=new URLSearchParams(location.search).get('id');const page=document.getElementById('page');if(!id){page.innerHTML=Utils.emptyState('Employee ID is missing');return}try{const r=await API.get('/api/employees/'+id),e=r.data;page.innerHTML=`<div class="page-header"><div><h1 class="page-title">Employee Profile</h1><p class="page-subtitle">View employee details and workforce summary</p></div><div style="display:flex;gap:8px"><button class="btn" id="edit"><i data-lucide="pencil"></i>Edit Employee</button><button class="btn btn-danger" id="delete"><i data-lucide="trash-2"></i>Delete</button></div></div><div class="profile-section" style="margin-bottom:18px"><div style="display:flex;gap:18px;align-items:center"><div class="big-avatar" style="width:90px;height:90px">${e.profilePhoto?`<img src="${Utils.esc(e.profilePhoto)}">`:Utils.initials(e.firstName+' '+e.lastName)}</div><div><h2 style="margin:0 0 6px">${Utils.esc(e.firstName+' '+e.lastName)}</h2><div class="muted">${Utils.esc(e.position)} · ${Utils.esc(e.department?.name||'—')}</div><div style="margin-top:10px">${Utils.statusBadge(e.status)} <span class="tag">${Utils.esc(e.employmentType)}</span></div></div></div></div><div class="grid activity-grid"><div class="profile-section"><h3 class="section-title">Personal Information</h3><div class="detail-grid"><div class="detail-item"><div class="label">Employee ID</div><div class="value">${Utils.esc(e.employeeId)}</div></div><div class="detail-item"><div class="label">Gender</div><div class="value">${Utils.esc(e.gender)}</div></div><div class="detail-item"><div class="label">Date of Birth</div><div class="value">${Utils.dateFmt(e.dateOfBirth)}</div></div><div class="detail-item"><div class="label">Joining Date</div><div class="value">${Utils.dateFmt(e.joiningDate)}</div></div></div></div><div class="profile-section"><h3 class="section-title">Contact Information</h3><div class="detail-grid"><div class="detail-item"><div class="label">Email</div><div class="value">${Utils.esc(e.email)}</div></div><div class="detail-item"><div class="label">Phone</div><div class="value">${Utils.esc(e.phone||'—')}</div></div><div class="detail-item"><div class="label">Address</div><div class="value">${Utils.esc(e.address||'—')}</div></div><div class="detail-item"><div class="label">Emergency</div><div class="value">${Utils.esc(e.emergencyContact?.name||'—')} ${Utils.esc(e.emergencyContact?.phone||'')}</div></div></div></div><div class="profile-section"><h3 class="section-title">Employment & Salary</h3><div class="detail-grid"><div class="detail-item"><div class="label">Department</div><div class="value">${Utils.esc(e.department?.name||'—')}</div></div><div class="detail-item"><div class="label">Role</div><div class="value">${Utils.esc(e.role)}</div></div><div class="detail-item"><div class="label">Salary</div><div class="value">${Utils.money(e.salary)}</div></div></div></div><div class="profile-section"><h3 class="section-title">Leave Balance</h3><div class="detail-grid"><div class="detail-item"><div class="label">Sick</div><div class="value">${e.leaveBalances?.sick??0} days</div></div><div class="detail-item"><div class="label">Casual</div><div class="value">${e.leaveBalances?.casual??0} days</div></div><div class="detail-item"><div class="label">Annual</div><div class="value">${e.leaveBalances?.annual??0} days</div></div></div></div></div>`;lucide.createIcons();document.getElementById('delete').onclick=async()=>{if(!confirm('Delete this employee and login account? This cannot be undone.'))return;try{await API.del('/api/employees/'+id);Utils.toast('Employee deleted');setTimeout(()=>location.href='employees.html',600)}catch(x){Utils.toast(x.message,'error')}};document.getElementById('edit').onclick=()=>location.href='employees.html?edit=${id}';}catch(x){page.innerHTML=Utils.emptyState(x.message)}})();
